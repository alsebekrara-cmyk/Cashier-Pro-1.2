# 🚀 دليل الاستخدام السريع - Quick Usage Guide

## نظام نقاط البيع المتقدم - النسخة المقسمة

---

## ⚡ التشغيل السريع (5 خطوات)

```bash
# 1. فك الضغط
unzip pos-system-complete.zip

# 2. الدخول للمجلد
cd pos-system-complete

# 3. تثبيت المتطلبات
npm install

# 4. التشغيل
npm start

# ✅ انتهى! التطبيق يعمل الآن
```

---

## 📂 أي ملف أستخدم؟

### ✅ للاستخدام المباشر (موصى به)
**`index-full.html`** 
- يحتوي على كل الكود (29,420 سطر)
- لا يحتاج ملفات خارجية
- يعمل مباشرة
- **Electron يستخدمه تلقائياً**

### 📝 للتطوير والتعديل
**`index.html`** + الملفات المنفصلة
- `core/main-functions.js` - الدوال الرئيسية
- `core/app-logic.js` - منطق التطبيق
- `styles/main-extracted.css` - الأنماط
- `index-body.html` - HTML

### 📖 للمرجع
**`index-original.html`**
- نسخة من الملف الأصلي
- للمقارنة والرجوع

---

## 🔧 التعديلات الشائعة

### تغيير معلومات المتجر:

ابحث في `core/main-functions.js` عن:
```javascript
const storeName = 'معرض يعقوب';
const storeAddress = 'باب الهاشمية';
const storePhone = '07803092185';
```

### تغيير الألوان:

ابحث في `styles/main-extracted.css` عن:
```css
:root {
    --primary-color: #667eea;
    --secondary-color: #38ef7d;
}
```

### إضافة صفحة جديدة:

1. **أضف HTML في `index-body.html`:**
```html
<div id="mypage" class="page">
    <h1>صفحتي الجديدة</h1>
</div>
```

2. **أضف رابط في الشريط الجانبي:**
```html
<div class="nav-item">
    <a onclick="showPage('mypage')">صفحتي</a>
</div>
```

3. **أضف JavaScript في `core/app-logic.js`:**
```javascript
// منطق صفحتي الجديدة
function initMyPage() {
    console.log('تهيئة صفحتي');
}
```

---

## 🎯 الملفات المهمة

| الملف | الوصف | متى تستخدمه |
|-------|--------|-------------|
| `index-full.html` | كل الكود | للتشغيل ✅ |
| `core/main-functions.js` | الدوال الرئيسية | للتعديل |
| `core/app-logic.js` | المنطق | للتعديل |
| `styles/main-extracted.css` | CSS | للتعديل |
| `index-body.html` | HTML | للتعديل |

---

## 📊 إحصائيات المشروع

- **الملف الأصلي:** 30,342 سطر
- **بعد التقسيم:**
  - main-functions.js: 341 سطر
  - app-logic.js: 19,657 سطر
  - main-extracted.css: 6,686 سطر
  - index-body.html: 3,366 سطر
- **المجموع:** 30,050 سطر ✅

---

## 🔄 إعادة البناء

إذا عدّلت الملفات المنفصلة وتريد دمجها:

```bash
# تشغيل سكريبت البناء
bash build-single-file.sh

# الناتج: index-rebuilt.html
```

---

## 💡 نصائح

### ✅ افعل
- استخدم `index-full.html` للتشغيل المباشر
- عدّل الملفات المنفصلة للتطوير
- احفظ نسخة احتياطية قبل التعديل

### ❌ لا تفعل
- لا تعدّل `index-full.html` مباشرة (عدّل الملفات المنفصلة ثم أعد البناء)
- لا تحذف `index-original.html` (مرجع مهم)

---

## 🆘 حل المشاكل

### المشكلة: التطبيق لا يشتغل
**الحل:**
```bash
# امسح node_modules وأعد التثبيت
rm -rf node_modules
npm install
npm start
```

### المشكلة: Firebase لا يعمل
**الحل:**
- تحقق من اتصال الإنترنت
- راجع `firebase-config.js`

### المشكلة: تعديلاتي لا تظهر
**الحل:**
- تأكد أنك عدّلت `index-full.html` أو
- عدّل الملفات المنفصلة ثم أعد البناء:
```bash
bash build-single-file.sh
```

---

## 📞 الدعم

**Digital Creativity - كرار السعبري**
- 📧 info@digital-creativity.iq
- 📱 07803092185
- 🌐 https://digital-creativity.iq

---

## 🎉 البدء

```bash
# الأمر الوحيد الذي تحتاجه:
npm start
```

**✅ بهذا البساطة!**

---

**النسخة:** 2.0.3  
**التاريخ:** 27 ديسمبر 2025  
**المطور:** كرار السعبري - شركة الإبداع الرقمي
