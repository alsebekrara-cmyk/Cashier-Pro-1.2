# نظام نقاط البيع المتقدم - POS System Pro

## 📋 نظرة عامة

نظام نقاط البيع المتقدم هو تطبيق سطح مكتب مبني باستخدام Electron لإدارة المبيعات والمخزون والديون في المتاجر والمعارض.

**الشركة المطورة:** شركة الإبداع الرقمي (Digital Creativity)  
**النسخة:** 2.0.3  
**المطور:** كرار السعبري

---

## 🏗️ البنية المعمارية الجديدة

تم إعادة هيكلة المشروع لتسهيل الصيانة والتطوير:

```
pos-system-refactored/
├── core/                    # الملفات الأساسية
│   ├── app.js              # التطبيق الرئيسي
│   ├── page-manager.js     # مدير الصفحات
│   └── utils.js            # الدوال المساعدة
├── pages/                   # صفحات التطبيق
│   ├── pos.js              # نقطة البيع
│   ├── products.js         # إدارة المنتجات
│   ├── invoices.js         # الفواتير
│   ├── debts.js            # إدارة الديون
│   ├── payments.js         # التسديدات
│   ├── expenses.js         # المصاريف
│   ├── receipts.js         # الإيصالات
│   ├── inventory.js        # المخزن
│   ├── printer.js          # الطابعة
│   └── settings.js         # الإعدادات
├── styles/                  # ملفات CSS
│   ├── main.css            # الأنماط الرئيسية
│   └── enhanced-modals.css # أنماط النوافذ المنبثقة
├── _sdk/                    # مكتبات SDK
│   ├── element_sdk.js      # SDK الإعدادات
│   └── data_sdk.js         # SDK البيانات
├── assets/                  # الصور والموارد
├── database/                # قاعدة البيانات
├── index.html              # الصفحة الرئيسية
├── main.js                 # ملف Electron الرئيسي
├── preload.js              # Preload Script
├── firebase-config.js      # إعدادات Firebase
├── reports-system.js       # نظام التقارير
├── expenses-system.js      # نظام المصاريف
├── yaqoub-print-templates.js # قوالب الطباعة
└── package.json            # إعدادات المشروع
```

---

## 🚀 البدء السريع

### المتطلبات الأساسية

- Node.js (v16 أو أحدث)
- npm أو yarn

### التثبيت

```bash
# 1. نسخ المشروع
cd pos-system-refactored

# 2. تثبيت الحزم
npm install

# 3. تشغيل التطبيق في وضع التطوير
npm run dev

# 4. بناء التطبيق للإنتاج
npm run build
```

---

## 📚 دليل الاستخدام

### إضافة صفحة جديدة

1. **إنشاء ملف الصفحة** في مجلد `pages/`:

```javascript
// pages/my-new-page.js
(function() {
    'use strict';

    const MyNewPage = {
        name: 'mynewpage',
        initialized: false,

        async init() {
            if (this.initialized) return;
            console.log('🔄 تهيئة صفحتي الجديدة...');
            this.setupEvents();
            this.initialized = true;
        },

        onShow() {
            console.log('👁️ عرض صفحتي الجديدة');
            // تنفيذ عند عرض الصفحة
        },

        onHide() {
            console.log('👋 إخفاء صفحتي الجديدة');
            // تنفيذ عند إخفاء الصفحة
        },

        setupEvents() {
            // إعداد الأحداث
        },

        async refresh() {
            // تحديث الصفحة
        }
    };

    if (typeof window.pageManager !== 'undefined') {
        window.pageManager.registerPage('mynewpage', MyNewPage);
    }
    
    window.myNewPage = MyNewPage;
    MyNewPage.init();
})();
```

2. **إضافة الصفحة إلى index.html**:

```html
<!-- في قسم الصفحات -->
<div id="mynewpage" class="page">
    <div class="page-header">
        <h2>صفحتي الجديدة</h2>
    </div>
    <div class="page-body">
        <!-- محتوى الصفحة -->
    </div>
</div>

<!-- في قسم السكريبتات -->
<script src="pages/my-new-page.js"></script>
```

3. **إضافة رابط في الشريط الجانبي**:

```html
<div class="nav-item">
    <a class="nav-link" onclick="showPage('mynewpage')">
        <i class="fas fa-star nav-icon"></i>
        <span class="nav-text">صفحتي الجديدة</span>
    </a>
</div>
```

---

## 🔧 الدوال المساعدة

تتوفر مجموعة من الدوال المساعدة في `core/utils.js`:

```javascript
// تخزين البيانات
await saveToLocalStorage('key', value);
const data = getFromLocalStorage('key', defaultValue);

// التنسيق
const formatted = formatCurrency(1000); // "1,000 د.ع"
const date = formatDate(new Date()); // "الأحد، 27 ديسمبر 2025"

// الإشعارات
showToast('رسالة نجاح', 'success');
showToast('رسالة خطأ', 'error');
showLoading(true, 'جاري التحميل...');

// أخرى
const id = generateUniqueId();
await copyToClipboard('نص للنسخ');
await delay(1000); // تأخير ثانية واحدة
```

---

## 🎨 التخصيص

### تغيير الألوان

عدّل المتغيرات في `styles/main.css`:

```css
:root {
    --primary-color: #667eea;
    --secondary-color: #38ef7d;
    --success-color: #10b981;
    --danger-color: #ef4444;
}
```

### إضافة سمة جديدة

```javascript
// في core/app.js
toggleTheme() {
    this.theme = this.theme === 'light' ? 'dark' : 'light';
    document.documentElement.setAttribute('data-theme', this.theme);
}
```

---

## 🐛 حل المشاكل

### المشكلة: الصفحة لا تعمل

**الحل:**
1. تأكد من تسجيل الصفحة في `page-manager.js`
2. تحقق من وجود ملف الصفحة في مجلد `pages/`
3. تأكد من تضمين السكريبت في `index.html`

### المشكلة: Firebase لا يعمل

**الحل:**
1. تحقق من صحة إعدادات Firebase في `firebase-config.js`
2. تأكد من اتصال الإنترنت
3. راجع وحدة التحكم (Console) للأخطاء

---

## 📝 ملاحظات مهمة

### البنية الجديدة

- ✅ كل صفحة في ملف منفصل
- ✅ الدوال المشتركة في `core/utils.js`
- ✅ إدارة مركزية للصفحات عبر `page-manager.js`
- ✅ تنظيم أفضل للكود
- ✅ سهولة الصيانة والتطوير

### الترقية من النسخة القديمة

إذا كنت تستخدم النسخة القديمة (ملف واحد كبير):
1. انسخ البيانات من localStorage
2. ثبّت النسخة الجديدة
3. استورد البيانات

---

## 🤝 المساهمة

للمساهمة في تطوير المشروع:

1. Fork المشروع
2. أنشئ فرع جديد (`git checkout -b feature/amazing-feature`)
3. Commit التغييرات (`git commit -m 'إضافة ميزة رائعة'`)
4. Push للفرع (`git push origin feature/amazing-feature`)
5. افتح Pull Request

---

## 📞 الدعم

للدعم الفني أو الاستفسارات:

- **البريد الإلكتروني:** info@digital-creativity.iq
- **الهاتف:** 07803092185
- **الموقع:** https://digital-creativity.iq

---

## 📜 الترخيص

هذا المشروع مرخص تحت رخصة MIT. راجع ملف `LICENSE` للمزيد من التفاصيل.

---

## 🙏 شكر وتقدير

- **Electron** - إطار العمل الأساسي
- **Firebase** - قاعدة البيانات السحابية
- **Font Awesome** - الأيقونات
- **Cairo Font** - الخط العربي

---

**تم التطوير بواسطة:** شركة الإبداع الرقمي - إدارة كرار السعبري  
**النسخة:** 2.0.3  
**آخر تحديث:** ديسمبر 2025
