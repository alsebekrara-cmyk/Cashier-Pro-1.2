# 🚀 دليل البدء السريع - Quick Start Guide

## نظام نقاط البيع المتقدم v2.0.3

---

## 📦 محتويات المشروع

تم تقسيم المشروع إلى ملفات منفصلة لسهولة الصيانة:

### 📁 المجلدات الرئيسية

- **`core/`** - الملفات الأساسية للتطبيق
  - `app.js` - إدارة التطبيق الرئيسية
  - `page-manager.js` - إدارة الصفحات والتنقل
  - `utils.js` - الدوال المساعدة المشتركة

- **`pages/`** - صفحات التطبيق المنفصلة
  - كل زر في الشريط الجانبي له ملف خاص
  - سهولة إضافة صفحات جديدة
  - كود منظم وسهل الصيانة

- **`styles/`** - ملفات الأنماط
  - `main.css` - الأنماط الرئيسية
  - `enhanced-modals.css` - أنماط النوافذ المنبثقة

---

## ⚡ التثبيت والتشغيل

### 1️⃣ التثبيت

```bash
# فك الضغط عن المشروع
unzip pos-system-refactored.zip
cd pos-system-refactored

# تثبيت المتطلبات
npm install
```

### 2️⃣ التشغيل

```bash
# وضع التطوير
npm run dev

# بناء للإنتاج
npm run build

# بناء نسخة محمولة
npm run build-win-portable
```

---

## 🔧 إضافة صفحة جديدة (خطوة بخطوة)

### الخطوة 1: إنشاء ملف الصفحة

أنشئ ملف جديد في `pages/` مثلاً `pages/reports.js`:

```javascript
(function() {
    'use strict';

    const ReportsPage = {
        name: 'reports',
        initialized: false,

        async init() {
            if (this.initialized) return;
            console.log('🔄 تهيئة صفحة التقارير...');
            
            // إعداد الأحداث
            this.setupEvents();
            
            this.initialized = true;
            console.log('✅ تم تهيئة صفحة التقارير');
        },

        onShow() {
            console.log('👁️ عرض صفحة التقارير');
            // يتم تنفيذها عند عرض الصفحة
        },

        onHide() {
            console.log('👋 إخفاء صفحة التقارير');
            // يتم تنفيذها عند إخفاء الصفحة
        },

        setupEvents() {
            // إعداد الأحداث والأزرار
            const generateBtn = document.getElementById('generateReportBtn');
            if (generateBtn) {
                generateBtn.addEventListener('click', () => {
                    this.generateReport();
                });
            }
        },

        generateReport() {
            console.log('توليد التقرير...');
            // منطق توليد التقرير
        },

        async refresh() {
            // تحديث محتوى الصفحة
            console.log('تحديث صفحة التقارير');
        }
    };

    // تسجيل الصفحة في مدير الصفحات
    if (typeof window.pageManager !== 'undefined') {
        window.pageManager.registerPage('reports', ReportsPage);
    }
    
    // تصدير للاستخدام العام
    window.reportsPage = ReportsPage;
    
    // تهيئة الصفحة
    ReportsPage.init();
    
    console.log('✅ تم تحميل صفحة التقارير');
})();
```

### الخطوة 2: إضافة HTML في index.html

أضف في قسم الصفحات (داخل `<div class="page-content">`):

```html
<!-- صفحة التقارير -->
<div id="reports" class="page">
    <div class="page-header">
        <h2><i class="fas fa-chart-bar"></i> التقارير</h2>
        <button id="generateReportBtn" class="btn btn-primary">
            <i class="fas fa-plus"></i> توليد تقرير
        </button>
    </div>
    <div class="page-body">
        <!-- محتوى الصفحة -->
        <p>محتوى صفحة التقارير هنا</p>
    </div>
</div>
```

### الخطوة 3: إضافة السكريبت في index.html

أضف في نهاية `<body>` قبل الإغلاق:

```html
<!-- Page Scripts -->
<script src="pages/reports.js"></script>
```

### الخطوة 4: إضافة زر في الشريط الجانبي

أضف في قائمة التنقل (داخل `<nav class="sidebar">`):

```html
<div class="nav-item" id="reportsNavItem">
    <a class="nav-link" onclick="showPage('reports')">
        <i class="fas fa-chart-bar nav-icon"></i>
        <span class="nav-text">التقارير</span>
    </a>
</div>
```

**✅ انتهى! صفحتك الجديدة جاهزة الآن**

---

## 🛠️ الدوال المساعدة الجاهزة

جميع هذه الدوال متوفرة في `core/utils.js`:

### 💾 التخزين المحلي

```javascript
// حفظ البيانات
await saveToLocalStorage('products', productsArray);

// استرجاع البيانات
const products = getFromLocalStorage('products', []);
```

### 📊 التنسيق

```javascript
// تنسيق العملة
formatCurrency(25000); // "25,000 د.ع"

// تنسيق التاريخ
formatDate(new Date()); // "الأحد، 27 ديسمبر 2025"
formatDate(new Date(), true); // مع الوقت

// تحويل الأرقام
toEnglishDigits('١٢٣'); // "123"
toArabicDigits('123'); // "١٢٣"
```

### 📣 الإشعارات

```javascript
// عرض رسالة نجاح
showToast('تمت العملية بنجاح', 'success');

// عرض رسالة خطأ
showToast('حدث خطأ', 'error');

// عرض تحذير
showToast('انتبه!', 'warning');

// شاشة تحميل
showLoading(true, 'جاري التحميل...');
showLoading(false); // إخفاء
```

### ✅ التحقق

```javascript
// التحقق من رقم هاتف عراقي
isValidIraqiPhone('07701234567'); // true

// التحقق من البريد الإلكتروني
isValidEmail('test@example.com'); // true
```

### 🔧 أدوات أخرى

```javascript
// توليد معرف فريد
const id = generateUniqueId();

// نسخ للحافظة
await copyToClipboard('نص للنسخ');

// تأخير
await delay(2000); // تأخير 2 ثانية

// Debounce للبحث
const debouncedSearch = debounce(searchFunction, 300);
```

---

## 🎨 تخصيص الألوان

عدّل الألوان في `styles/main.css`:

```css
:root {
    /* ألوان أساسية */
    --primary-color: #667eea;      /* اللون الرئيسي */
    --secondary-color: #38ef7d;    /* اللون الثانوي */
    --success-color: #10b981;      /* لون النجاح */
    --danger-color: #ef4444;       /* لون الخطر */
    --warning-color: #f59e0b;      /* لون التحذير */
    
    /* ألوان الخلفية */
    --bg-color: #f8fafc;           /* خلفية عامة */
    --surface-color: #ffffff;       /* خلفية البطاقات */
    
    /* ألوان النصوص */
    --text-primary: #1f2937;       /* نص رئيسي */
    --text-secondary: #6b7280;     /* نص ثانوي */
}
```

---

## 📱 الوضع المتجاوب (Responsive)

التطبيق يعمل على جميع الأحجام:

- 🖥️ **سطح المكتب** - كامل الميزات
- 📱 **الهواتف** - قائمة منزلقة
- 📲 **الأجهزة اللوحية** - تصميم متكيف

---

## 🔒 الأمان

- ✅ جميع البيانات محفوظة محلياً (localStorage)
- ✅ دعم Firebase للمزامنة السحابية (اختياري)
- ✅ إمكانية إنشاء نسخ احتياطية

---

## 🐛 حل المشاكل الشائعة

### المشكلة: الصفحة لا تظهر

**الحل:**
1. تأكد من تضمين ملف الصفحة في `index.html`
2. تحقق من Console للأخطاء (F12)
3. تأكد من تسجيل الصفحة في `page-manager`

### المشكلة: الدوال المساعدة لا تعمل

**الحل:**
1. تأكد من تحميل `core/utils.js` أولاً
2. تحقق من الترتيب الصحيح للسكريبتات في `index.html`

### المشكلة: Firebase لا يعمل

**الحل:**
1. تحقق من إعدادات Firebase في `firebase-config.js`
2. تأكد من اتصال الإنترنت
3. راجع Console للأخطاء

---

## 📞 الدعم الفني

**شركة الإبداع الرقمي**

- 📧 البريد: info@digital-creativity.iq
- 📱 الهاتف: 07803092185
- 🌐 الموقع: https://digital-creativity.iq

---

## 📝 ملاحظات نهائية

### ✅ مزايا البنية الجديدة

1. **سهولة الصيانة** - كل صفحة في ملف منفصل
2. **تنظيم أفضل** - كود واضح ومنظم
3. **سهولة التطوير** - إضافة صفحات جديدة بسهولة
4. **أداء محسّن** - تحميل أسرع
5. **قابلية التوسع** - سهولة إضافة ميزات جديدة

### 📚 الموارد المفيدة

- [Electron Documentation](https://www.electronjs.org/docs)
- [Firebase Documentation](https://firebase.google.com/docs)
- [MDN Web Docs](https://developer.mozilla.org/ar/)

---

**تم تطويره بواسطة:** كرار السعبري - شركة الإبداع الرقمي  
**النسخة:** 2.0.3  
**التاريخ:** 27 ديسمبر 2025

---

💡 **نصيحة:** احفظ هذا الملف كمرجع سريع أثناء التطوير!
